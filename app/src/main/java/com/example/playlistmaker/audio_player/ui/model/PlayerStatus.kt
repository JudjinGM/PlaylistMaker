package com.example.playlistmaker.audio_player.ui.model

enum class PlayerStatus {
    STATE_DEFAULT,
    STATE_PREPARED,
    STATE_PLAYING,
    STATE_PAUSED,
    STATE_ERROR,
    STATE_NETWORK_ERROR,
}